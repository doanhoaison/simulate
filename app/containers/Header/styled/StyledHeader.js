import styled from 'styled-components';

export default styled.header`
  .bp3-navbar {
    padding-left: 50px;
    padding-right: 50px;
  }
  .btn-group {
    display: flex;
    height: 100%;
    .dropdown-toggle {
      border: none;
      color: black;
      font-size: 14px;
    }
  }
`;
