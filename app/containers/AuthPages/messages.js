/*
 * AuthPages Messages
 *
 * This contains all the text for the AuthPages container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.AuthPages";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the AuthPages container!"
  }
});
